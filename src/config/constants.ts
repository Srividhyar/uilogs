export enum severityRank {
    CRITICAL = 0,
    ERROR = 1,
    WARN = 2,
    INFO = 3,
    DEBUG = 4
}

export enum division {
    bCom = "Blooming",
    mCom = "MAcys"
}

export enum environment {
    DEV = "DEV",
    QA = "QA",
    UAT = "UAT",
    PILOT = "PILOT",
    PROD = "PROD"
}
